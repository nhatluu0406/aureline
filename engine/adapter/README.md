# Forge launch adapter

Adapter production tối thiểu chạy trước bind, đọc đúng một secret frame từ inherited stdin pipe, cài outer ASGI guard cho HTTP/WebSocket và fail closed khi seam Gradio 4.40.0 thay đổi. Token không đi qua argv, environment hay file.
